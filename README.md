## как запустить локально

```bash
cd src
npm install
npm run start
```
проект запустится на таком адресе `http://localhost:8080/` 

## как собрать проект
из директории src ввести следующую команду
```bash
npm run build
```

собранный проект появится в `src/dist`

## что бы сделал, но не успел

1. изображения в одну папку переместил
2. донастроил бы scss, чтобы не надо было вручную собирать (не успел)
3. сделать декомпозицию файла `src/js/app.js` & `src/css/input.scss`

## уточнения
хоть в ТЗ и указанно, что использовать сторонние библиотеки не нужно, я считаю, что при работе чисто с HTML + CSS + JS сборщик - необходим.
